generator client {
  provider     = "prisma-client"
  output       = "../../src/common/database/infrastructure/prisma-client"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

generator erd {
  provider = "prisma-erd-generator"
  output   = "../../README_DATABASE.md"
}
