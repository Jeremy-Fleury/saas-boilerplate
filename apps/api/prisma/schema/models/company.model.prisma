model Company {
    // Business
    id   String @id @default(uuid())
    name String @unique

    // Relationships
    examples Example[]

    // Metadata
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    // Indexes
    @@index([id])
}
